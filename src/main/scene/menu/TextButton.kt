package main.scene.menu

import Phaser.GameObjects.Text
import Phaser.Scene
import Phaser.Types.GameObjects.Text.TextStyle

class TextButton(scene: Scene, x: Number, y: Number, text: String, style: TextStyle) :
    Text(scene, x, y, text, style) {

    init {

    }
}